{
  "name": "@bevyl-ai/agent-tools",
  "version": "0.4.1",
  "description": "Shared runtime contract + generic host tools for codex-app-server agents. Codex/exe.dev only \u2014 never the Claude API.",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/bevyl-ai/agent-tools"
  },
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "types": "./src/index.ts",
  "files": [
    "src",
    "README.md",
    "LICENSE"
  ],
  "engines": {
    "bun": ">=1.3"
  },
  "scripts": {
    "test": "bun test",
    "typecheck": "tsc -p tsconfig.json",
    "check": "bun run typecheck && bun test"
  },
  "devDependencies": {
    "@types/bun": "^1.2.0",
    "typescript": "^5.6.0"
  }
}
